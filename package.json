{
  "name": "gruppenplatz-challenge",
  "scripts": {
    "test": "playwright test",
    "test:allure": "playwright test --reporter=line,allure-playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "allure-playwright": "^3.0.0",
    "allure": "^3.0.0"
  }
}